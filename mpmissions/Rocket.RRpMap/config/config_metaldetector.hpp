
class CfgMetalDetector {
	detector_color_default = "1,0.956863,0.592157"; // Detector default color | RGB/255 format
	detector_color_notdetected = "0.27451,1,0.458824"; // Detector color when weapon is not detected | RGB/255 format
	detector_color_detected = "0.960784,0.337255,0.286275"; // Detector color when weapon is detected | RGB/255 format
	detector_sound_notdetected = "detector_success"; // Detector sound when weapon is not detected
	detector_sound_detected = "detector_failure"; // Detector sound when weapon is detected
	detect_magazines = 0; // Set whether detector should find magazines or not
};
